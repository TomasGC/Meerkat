CREATE FUNCTION GetUserCount() RETURNS INT
BEGIN
    RETURN (SELECT COUNT(*) FROM users);
END;

CREATE OR REPLACE FUNCTION NormalizeEmail(p_email VARCHAR(255)) RETURNS VARCHAR(255)
BEGIN
    RETURN LOWER(TRIM(p_email));
END;
